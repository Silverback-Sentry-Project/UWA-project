# Base URL of the WildWatch Admin API (Laravel backend), including /api
# Use `auto` to derive the API URL from the browser hostname (LAN-friendly).
# Or set an explicit URL, e.g. VITE_API_URL=http://192.168.1.42:8000/api
VITE_API_URL=auto
